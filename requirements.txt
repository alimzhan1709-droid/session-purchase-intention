# Reference environment declared in Appendix B of the thesis.
# These are the versions under which the reported tables and figures were produced;
# pin to them to reproduce the published numbers exactly.
# Python 3.13.5

pandas==2.2.3
numpy==2.3.5
scikit-learn==1.8.0
lightgbm==4.6.0
scipy==1.17.0
matplotlib==3.10.8

# Required only by thesis_gaps_addon.py and thesis_protocol_contrast.py,
# which use SMOTE to reproduce the prior-work evaluation protocol.
imbalanced-learn==0.12.4
